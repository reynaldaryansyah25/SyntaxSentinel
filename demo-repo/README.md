# SyntaxSentinel Demo Repository

This is a small healthy Python repository for the SyntaxSentinel demo.

The default state is intentionally healthy. The GitLab CI pipeline should pass.

## Run Locally

```bash
pip install -r requirements.txt
pytest -q